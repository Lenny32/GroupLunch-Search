{
  "name": "group-lunch-search",
  "version": "0.1.0",
  "description": "LLM-orchestrated Playwright runner to find and extract lunch menus",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js --config config.json",
    "dev": "node src/index.js --config config.dev.json"
  },
  "dependencies": {
    "ajv": "^8.12.0",
    "openai": "^4.0.0",
    "playwright": "^1.49.0"
  }
}